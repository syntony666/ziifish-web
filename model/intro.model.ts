export interface IntroModel {
    id: string
    name: string,
    description: JSX.Element,
    twitterUrl?: string,
    twitchUrl?: string,
    youtubeUrl?: string,
    instagramUrl?: string,
    facebookUrl?: string
};
